# Production configs / logs
.env
.env.local
*.log

# Node artifact files
node_modules/
dist/
build/
coverage/

# Generated by IDE/MacOS/Windows
.idea/
.DS_Store
Thumbs.db

# Personal local md, json
*.local.md
*.local.json

# Agent working directory — session transcripts, scratch notes, local settings.
# Ignored here rather than only in this machine's global excludes, so a fresh
# clone cannot commit it by accident.
.claude/